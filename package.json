{
  "name": "ploydok",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "check:spdx": "bun run scripts/check-spdx.ts",
    "openapi:docs": "NODE_ENV=test bun run scripts/generate-openapi.ts",
    "db:migrate": "bun run --cwd packages/db drizzle-kit migrate",
    "db:generate": "bun run --cwd packages/db drizzle-kit generate",
    "audit:rootless": "bash scripts/audit-rootless.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "postgres": "^3.4.9",
    "prettier": "^3.8.1",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "turbo": "^2.8.17",
    "typescript": "5.9.3"
  },
  "packageManager": "bun@1.3.11",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/*"
  ]
}
