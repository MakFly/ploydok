# Generated by Ploydok installer for coexist mode.
# Requires: a2enmod proxy proxy_http proxy_wstunnel headers

ProxyPreserveHost On
ProxyPass / http://127.0.0.1:${PLOYDOK_HTTP_PORT}/ retry=0
ProxyPassReverse / http://127.0.0.1:${PLOYDOK_HTTP_PORT}/
RequestHeader set X-Forwarded-Proto "https"
