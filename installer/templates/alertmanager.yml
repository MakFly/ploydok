# ploydok-monitoring-schema: 1
route:
  receiver: operator-webhook
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: operator-webhook
    webhook_configs:
      - send_resolved: true
        url_file: /run/ploydok-secrets/alert-webhook-url
