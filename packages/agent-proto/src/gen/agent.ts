// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.21.12
// source: agent.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientDuplexStream,
  type ClientOptions,
  type ClientReadableStream,
  type ClientUnaryCall,
  type handleBidiStreamingCall,
  type handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "ploydok.agent.v1";

/** Volume mount binding. */
export interface VolumeMount {
  hostPath: string;
  containerPath: string;
  readOnly: boolean;
}

/** Port mapping. */
export interface PortMapping {
  containerPort: number;
  hostPort: number;
  /** "tcp" or "udp" — defaults to "tcp" if empty */
  proto: string;
}

/** CPU / memory / pids limits. Zero values mean "no limit". */
export interface ResourceLimits {
  /** Fractional CPUs, e.g. 0.5 = 50 % of one core */
  cpu: number;
  /** Memory limit in bytes */
  memoryBytes: number;
  /** Maximum number of PIDs (processes+threads) the container may spawn */
  pidsLimit: number;
}

export interface ContainerCreateRequest {
  /** Container name — MUST match the ploydok-* allowlist enforced by the agent. */
  name: string;
  /** Docker image reference (e.g. "nginx:1.25-alpine"). */
  image: string;
  /** Environment variables. Keys must not start with PLOYDOK_INTERNAL_. */
  env: { [key: string]: string };
  /** Labels applied to the container. */
  labels: { [key: string]: string };
  /**
   * DEPRECATED: use `networks` below. Kept for backward compat.
   * When `networks` is non-empty it takes precedence and this field is ignored.
   */
  network: string;
  /** Volume mounts. host_path must be under /var/lib/ploydok/volumes/. */
  volumes: VolumeMount[];
  /** Port mappings. */
  ports: PortMapping[];
  /** Restart policy: "", "no", "always", "unless-stopped", "on-failure". */
  restartPolicy: string;
  /** Resource limits applied to the container. */
  resourceLimits:
    | ResourceLimits
    | undefined;
  /** Override the image entrypoint/command. */
  command: string[];
  /** Run the container process as this user (e.g. "1000:1000"). */
  user: string;
  /**
   * Docker networks to attach the container to (each must be pre-created via NetworkCreate).
   * When non-empty this supersedes the deprecated `network` field above.
   */
  networks: string[];
}

export interface ContainerCreateRequest_EnvEntry {
  key: string;
  value: string;
}

export interface ContainerCreateRequest_LabelsEntry {
  key: string;
  value: string;
}

export interface ContainerCreateResponse {
  containerId: string;
}

export interface ContainerStartRequest {
  containerId: string;
}

export interface ContainerStartResponse {
}

export interface ContainerStopRequest {
  containerId: string;
  /** Seconds to wait before SIGKILL. 0 → use Docker default (10s). */
  timeoutSeconds: number;
}

export interface ContainerStopResponse {
}

export interface ContainerRemoveRequest {
  containerId: string;
  /** Remove the container even if running. */
  force: boolean;
  /** Remove anonymous volumes attached to the container. */
  removeVolumes: boolean;
}

export interface ContainerRemoveResponse {
}

export interface ContainerLogsRequest {
  containerId: string;
  /** Stream new lines (tail -f behaviour). */
  follow: boolean;
  /** Return logs since this Unix timestamp (0 = no lower bound). */
  sinceUnix: number;
  /** Maximum number of lines to return from the tail. 0 = all. */
  tail: number;
}

export interface LogLine {
  /** "stdout" or "stderr" */
  stream: string;
  line: string;
  /** Timestamp from Docker log driver (RFC 3339 with nano precision). */
  timestamp: string;
}

export interface ContainerStatsRequest {
  containerId: string;
  /** Stream stats continuously (like `docker stats`). False → single snapshot. */
  stream: boolean;
}

export interface StatsFrame {
  containerId: string;
  /** Unix timestamp (nanoseconds) of the sample. */
  timestampNs: number;
  /** CPU usage, normalised to [0.0, num_cpus]. */
  cpuPercent: number;
  /** Resident memory in bytes. */
  memoryBytes: number;
  /** Memory limit in bytes. */
  memoryLimitBytes: number;
  /** Network bytes received since container start. */
  netRxBytes: number;
  /** Network bytes transmitted since container start. */
  netTxBytes: number;
}

/** Registry authentication for private image pulls. */
export interface RegistryAuth {
  username: string;
  password: string;
}

export interface ImagePullRequest {
  /** Full image reference including tag (e.g. "nginx:1.25-alpine"). */
  image: string;
  /** Optional registry credentials. Omit for anonymous pulls. */
  registryAuth: RegistryAuth | undefined;
}

export interface PullProgress {
  status: string;
  layerId: string;
  current: number;
  total: number;
}

export interface ImageBuildRequest {
  /** Tag to apply to the resulting image. */
  tag: string;
  /** Dockerfile content (UTF-8). */
  dockerfile: Uint8Array;
  /** Build context files: path → content. */
  context: { [key: string]: Uint8Array };
  /** Build-time ARGs. */
  buildArgs: { [key: string]: string };
}

export interface ImageBuildRequest_ContextEntry {
  key: string;
  value: Uint8Array;
}

export interface ImageBuildRequest_BuildArgsEntry {
  key: string;
  value: string;
}

export interface BuildProgress {
  /** Raw JSON stream line from Docker build output. */
  stream: string;
  /** Non-empty when the build step emits an error. */
  error: string;
}

export interface NetworkCreateRequest {
  /** Network name — must match ploydok-* prefix. */
  name: string;
  /** Docker network driver (e.g. "bridge"). Defaults to "bridge" when empty. */
  driver: string;
  /** Optional labels. */
  labels: { [key: string]: string };
}

export interface NetworkCreateRequest_LabelsEntry {
  key: string;
  value: string;
}

export interface NetworkCreateResponse {
  networkId: string;
}

export interface NetworkRemoveRequest {
  networkId: string;
}

export interface NetworkRemoveResponse {
}

export interface ContainerSnapshot {
  id: string;
  name: string;
  image: string;
  /** "running" | "starting" | "unhealthy" | "stopped" | "unknown" */
  status: string;
  /** 0 if stopped */
  uptimeS: number;
  /** 0-100, sampled */
  cpuPct: number;
  memBytes: number;
  memLimitBytes: number;
  restartCount: number;
  /** Label "ploydok.kind" — "app" | "infra" | "agent" | "" (unknown) */
  kind: string;
  /** Label "ploydok.app_id" if kind=="app", empty otherwise */
  appId: string;
  /** Label "ploydok.color" if kind=="app" — "blue"|"green"|"" */
  color: string;
  /** Last active ping result (filled only after PingContainer called); 0 if never */
  lastPingMs: number;
  lastPingOk: boolean;
  /** Unix millis of last seen */
  lastSeenMs: number;
}

export interface ListContainersRequest {
  /** Optional: filter by label "ploydok.kind" */
  kindFilter: string;
}

export interface ListContainersResponse {
  containers: ContainerSnapshot[];
}

export interface PingContainerRequest {
  containerId: string;
  /** e.g. "/health" — mandatory, max 256 chars */
  path: string;
  /** Target port on the container (must match one of its exposed ports) */
  port: number;
  /** Timeout in ms, capped at 5000 server-side */
  timeoutMs: number;
}

export interface PingContainerResponse {
  ok: boolean;
  /** 0 if transport error */
  statusCode: number;
  latencyMs: number;
  /** empty if ok */
  error: string;
}

/**
 * Multiplexed frame for the ContainerExec bidi stream.
 * Client sends: ExecStart (first), then stdin / resize frames.
 * Server sends: ExecReady, then stdout / stderr / exit frames.
 */
export interface ExecFrame {
  start?: ExecStart | undefined;
  stdin?: Uint8Array | undefined;
  stdout?: Uint8Array | undefined;
  stderr?: Uint8Array | undefined;
  resize?: ExecResize | undefined;
  exit?: ExecExit | undefined;
  ready?: ExecReady | undefined;
}

/** First frame sent by the client — initialises the exec session. */
export interface ExecStart {
  /** Container name or id — must match the ploydok-* allowlist. */
  containerId: string;
  /** Command to run inside the container (e.g. ["/bin/sh"]). */
  cmd: string[];
  /** Allocate a pseudo-TTY. */
  tty: boolean;
  /** Initial terminal width in columns. */
  cols: number;
  /** Initial terminal height in rows. */
  rows: number;
  /** Run as this user (e.g. "1000:1000"). Empty → image default. */
  user: string;
}

/** Sent by the client to resize the terminal window. */
export interface ExecResize {
  cols: number;
  rows: number;
}

/** Sent by the server when the exec'd process exits. */
export interface ExecExit {
  code: number;
}

/** Sent by the server once the exec session is ready (after create+start). */
export interface ExecReady {
  /** Docker exec id — useful for debugging / correlation. */
  execId: string;
}

function createBaseVolumeMount(): VolumeMount {
  return { hostPath: "", containerPath: "", readOnly: false };
}

export const VolumeMount: MessageFns<VolumeMount> = {
  encode(message: VolumeMount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hostPath !== "") {
      writer.uint32(10).string(message.hostPath);
    }
    if (message.containerPath !== "") {
      writer.uint32(18).string(message.containerPath);
    }
    if (message.readOnly !== false) {
      writer.uint32(24).bool(message.readOnly);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VolumeMount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVolumeMount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hostPath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.containerPath = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.readOnly = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VolumeMount {
    return {
      hostPath: isSet(object.hostPath)
        ? globalThis.String(object.hostPath)
        : isSet(object.host_path)
        ? globalThis.String(object.host_path)
        : "",
      containerPath: isSet(object.containerPath)
        ? globalThis.String(object.containerPath)
        : isSet(object.container_path)
        ? globalThis.String(object.container_path)
        : "",
      readOnly: isSet(object.readOnly)
        ? globalThis.Boolean(object.readOnly)
        : isSet(object.read_only)
        ? globalThis.Boolean(object.read_only)
        : false,
    };
  },

  toJSON(message: VolumeMount): unknown {
    const obj: any = {};
    if (message.hostPath !== "") {
      obj.hostPath = message.hostPath;
    }
    if (message.containerPath !== "") {
      obj.containerPath = message.containerPath;
    }
    if (message.readOnly !== false) {
      obj.readOnly = message.readOnly;
    }
    return obj;
  },

  create(base?: DeepPartial<VolumeMount>): VolumeMount {
    return VolumeMount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VolumeMount>): VolumeMount {
    const message = createBaseVolumeMount();
    message.hostPath = object.hostPath ?? "";
    message.containerPath = object.containerPath ?? "";
    message.readOnly = object.readOnly ?? false;
    return message;
  },
};

function createBasePortMapping(): PortMapping {
  return { containerPort: 0, hostPort: 0, proto: "" };
}

export const PortMapping: MessageFns<PortMapping> = {
  encode(message: PortMapping, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerPort !== 0) {
      writer.uint32(8).uint32(message.containerPort);
    }
    if (message.hostPort !== 0) {
      writer.uint32(16).uint32(message.hostPort);
    }
    if (message.proto !== "") {
      writer.uint32(26).string(message.proto);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PortMapping {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePortMapping();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.containerPort = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.hostPort = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.proto = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PortMapping {
    return {
      containerPort: isSet(object.containerPort)
        ? globalThis.Number(object.containerPort)
        : isSet(object.container_port)
        ? globalThis.Number(object.container_port)
        : 0,
      hostPort: isSet(object.hostPort)
        ? globalThis.Number(object.hostPort)
        : isSet(object.host_port)
        ? globalThis.Number(object.host_port)
        : 0,
      proto: isSet(object.proto) ? globalThis.String(object.proto) : "",
    };
  },

  toJSON(message: PortMapping): unknown {
    const obj: any = {};
    if (message.containerPort !== 0) {
      obj.containerPort = Math.round(message.containerPort);
    }
    if (message.hostPort !== 0) {
      obj.hostPort = Math.round(message.hostPort);
    }
    if (message.proto !== "") {
      obj.proto = message.proto;
    }
    return obj;
  },

  create(base?: DeepPartial<PortMapping>): PortMapping {
    return PortMapping.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PortMapping>): PortMapping {
    const message = createBasePortMapping();
    message.containerPort = object.containerPort ?? 0;
    message.hostPort = object.hostPort ?? 0;
    message.proto = object.proto ?? "";
    return message;
  },
};

function createBaseResourceLimits(): ResourceLimits {
  return { cpu: 0, memoryBytes: 0, pidsLimit: 0 };
}

export const ResourceLimits: MessageFns<ResourceLimits> = {
  encode(message: ResourceLimits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cpu !== 0) {
      writer.uint32(9).double(message.cpu);
    }
    if (message.memoryBytes !== 0) {
      writer.uint32(16).int64(message.memoryBytes);
    }
    if (message.pidsLimit !== 0) {
      writer.uint32(24).int64(message.pidsLimit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceLimits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceLimits();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.cpu = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.memoryBytes = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pidsLimit = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceLimits {
    return {
      cpu: isSet(object.cpu) ? globalThis.Number(object.cpu) : 0,
      memoryBytes: isSet(object.memoryBytes)
        ? globalThis.Number(object.memoryBytes)
        : isSet(object.memory_bytes)
        ? globalThis.Number(object.memory_bytes)
        : 0,
      pidsLimit: isSet(object.pidsLimit)
        ? globalThis.Number(object.pidsLimit)
        : isSet(object.pids_limit)
        ? globalThis.Number(object.pids_limit)
        : 0,
    };
  },

  toJSON(message: ResourceLimits): unknown {
    const obj: any = {};
    if (message.cpu !== 0) {
      obj.cpu = message.cpu;
    }
    if (message.memoryBytes !== 0) {
      obj.memoryBytes = Math.round(message.memoryBytes);
    }
    if (message.pidsLimit !== 0) {
      obj.pidsLimit = Math.round(message.pidsLimit);
    }
    return obj;
  },

  create(base?: DeepPartial<ResourceLimits>): ResourceLimits {
    return ResourceLimits.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResourceLimits>): ResourceLimits {
    const message = createBaseResourceLimits();
    message.cpu = object.cpu ?? 0;
    message.memoryBytes = object.memoryBytes ?? 0;
    message.pidsLimit = object.pidsLimit ?? 0;
    return message;
  },
};

function createBaseContainerCreateRequest(): ContainerCreateRequest {
  return {
    name: "",
    image: "",
    env: {},
    labels: {},
    network: "",
    volumes: [],
    ports: [],
    restartPolicy: "",
    resourceLimits: undefined,
    command: [],
    user: "",
    networks: [],
  };
}

export const ContainerCreateRequest: MessageFns<ContainerCreateRequest> = {
  encode(message: ContainerCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.image !== "") {
      writer.uint32(18).string(message.image);
    }
    globalThis.Object.entries(message.env).forEach(([key, value]: [string, string]) => {
      ContainerCreateRequest_EnvEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    globalThis.Object.entries(message.labels).forEach(([key, value]: [string, string]) => {
      ContainerCreateRequest_LabelsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.network !== "") {
      writer.uint32(42).string(message.network);
    }
    for (const v of message.volumes) {
      VolumeMount.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.ports) {
      PortMapping.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.restartPolicy !== "") {
      writer.uint32(66).string(message.restartPolicy);
    }
    if (message.resourceLimits !== undefined) {
      ResourceLimits.encode(message.resourceLimits, writer.uint32(74).fork()).join();
    }
    for (const v of message.command) {
      writer.uint32(82).string(v!);
    }
    if (message.user !== "") {
      writer.uint32(90).string(message.user);
    }
    for (const v of message.networks) {
      writer.uint32(98).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ContainerCreateRequest_EnvEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.env[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = ContainerCreateRequest_LabelsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.labels[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.network = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.volumes.push(VolumeMount.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.ports.push(PortMapping.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.restartPolicy = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.resourceLimits = ResourceLimits.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.command.push(reader.string());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.user = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.networks.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerCreateRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      image: isSet(object.image) ? globalThis.String(object.image) : "",
      env: isObject(object.env)
        ? (globalThis.Object.entries(object.env) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      labels: isObject(object.labels)
        ? (globalThis.Object.entries(object.labels) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      network: isSet(object.network) ? globalThis.String(object.network) : "",
      volumes: globalThis.Array.isArray(object?.volumes) ? object.volumes.map((e: any) => VolumeMount.fromJSON(e)) : [],
      ports: globalThis.Array.isArray(object?.ports) ? object.ports.map((e: any) => PortMapping.fromJSON(e)) : [],
      restartPolicy: isSet(object.restartPolicy)
        ? globalThis.String(object.restartPolicy)
        : isSet(object.restart_policy)
        ? globalThis.String(object.restart_policy)
        : "",
      resourceLimits: isSet(object.resourceLimits)
        ? ResourceLimits.fromJSON(object.resourceLimits)
        : isSet(object.resource_limits)
        ? ResourceLimits.fromJSON(object.resource_limits)
        : undefined,
      command: globalThis.Array.isArray(object?.command)
        ? object.command.map((e: any) => globalThis.String(e))
        : [],
      user: isSet(object.user) ? globalThis.String(object.user) : "",
      networks: globalThis.Array.isArray(object?.networks)
        ? object.networks.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ContainerCreateRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.image !== "") {
      obj.image = message.image;
    }
    if (message.env) {
      const entries = globalThis.Object.entries(message.env) as [string, string][];
      if (entries.length > 0) {
        obj.env = {};
        entries.forEach(([k, v]) => {
          obj.env[k] = v;
        });
      }
    }
    if (message.labels) {
      const entries = globalThis.Object.entries(message.labels) as [string, string][];
      if (entries.length > 0) {
        obj.labels = {};
        entries.forEach(([k, v]) => {
          obj.labels[k] = v;
        });
      }
    }
    if (message.network !== "") {
      obj.network = message.network;
    }
    if (message.volumes?.length) {
      obj.volumes = message.volumes.map((e) => VolumeMount.toJSON(e));
    }
    if (message.ports?.length) {
      obj.ports = message.ports.map((e) => PortMapping.toJSON(e));
    }
    if (message.restartPolicy !== "") {
      obj.restartPolicy = message.restartPolicy;
    }
    if (message.resourceLimits !== undefined) {
      obj.resourceLimits = ResourceLimits.toJSON(message.resourceLimits);
    }
    if (message.command?.length) {
      obj.command = message.command;
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    if (message.networks?.length) {
      obj.networks = message.networks;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerCreateRequest>): ContainerCreateRequest {
    return ContainerCreateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerCreateRequest>): ContainerCreateRequest {
    const message = createBaseContainerCreateRequest();
    message.name = object.name ?? "";
    message.image = object.image ?? "";
    message.env = (globalThis.Object.entries(object.env ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.labels = (globalThis.Object.entries(object.labels ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.network = object.network ?? "";
    message.volumes = object.volumes?.map((e) => VolumeMount.fromPartial(e)) || [];
    message.ports = object.ports?.map((e) => PortMapping.fromPartial(e)) || [];
    message.restartPolicy = object.restartPolicy ?? "";
    message.resourceLimits = (object.resourceLimits !== undefined && object.resourceLimits !== null)
      ? ResourceLimits.fromPartial(object.resourceLimits)
      : undefined;
    message.command = object.command?.map((e) => e) || [];
    message.user = object.user ?? "";
    message.networks = object.networks?.map((e) => e) || [];
    return message;
  },
};

function createBaseContainerCreateRequest_EnvEntry(): ContainerCreateRequest_EnvEntry {
  return { key: "", value: "" };
}

export const ContainerCreateRequest_EnvEntry: MessageFns<ContainerCreateRequest_EnvEntry> = {
  encode(message: ContainerCreateRequest_EnvEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerCreateRequest_EnvEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerCreateRequest_EnvEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerCreateRequest_EnvEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ContainerCreateRequest_EnvEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerCreateRequest_EnvEntry>): ContainerCreateRequest_EnvEntry {
    return ContainerCreateRequest_EnvEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerCreateRequest_EnvEntry>): ContainerCreateRequest_EnvEntry {
    const message = createBaseContainerCreateRequest_EnvEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseContainerCreateRequest_LabelsEntry(): ContainerCreateRequest_LabelsEntry {
  return { key: "", value: "" };
}

export const ContainerCreateRequest_LabelsEntry: MessageFns<ContainerCreateRequest_LabelsEntry> = {
  encode(message: ContainerCreateRequest_LabelsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerCreateRequest_LabelsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerCreateRequest_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerCreateRequest_LabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ContainerCreateRequest_LabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerCreateRequest_LabelsEntry>): ContainerCreateRequest_LabelsEntry {
    return ContainerCreateRequest_LabelsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerCreateRequest_LabelsEntry>): ContainerCreateRequest_LabelsEntry {
    const message = createBaseContainerCreateRequest_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseContainerCreateResponse(): ContainerCreateResponse {
  return { containerId: "" };
}

export const ContainerCreateResponse: MessageFns<ContainerCreateResponse> = {
  encode(message: ContainerCreateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerCreateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerCreateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerCreateResponse {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
    };
  },

  toJSON(message: ContainerCreateResponse): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerCreateResponse>): ContainerCreateResponse {
    return ContainerCreateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerCreateResponse>): ContainerCreateResponse {
    const message = createBaseContainerCreateResponse();
    message.containerId = object.containerId ?? "";
    return message;
  },
};

function createBaseContainerStartRequest(): ContainerStartRequest {
  return { containerId: "" };
}

export const ContainerStartRequest: MessageFns<ContainerStartRequest> = {
  encode(message: ContainerStartRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStartRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStartRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerStartRequest {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
    };
  },

  toJSON(message: ContainerStartRequest): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerStartRequest>): ContainerStartRequest {
    return ContainerStartRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerStartRequest>): ContainerStartRequest {
    const message = createBaseContainerStartRequest();
    message.containerId = object.containerId ?? "";
    return message;
  },
};

function createBaseContainerStartResponse(): ContainerStartResponse {
  return {};
}

export const ContainerStartResponse: MessageFns<ContainerStartResponse> = {
  encode(_: ContainerStartResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStartResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStartResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ContainerStartResponse {
    return {};
  },

  toJSON(_: ContainerStartResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ContainerStartResponse>): ContainerStartResponse {
    return ContainerStartResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ContainerStartResponse>): ContainerStartResponse {
    const message = createBaseContainerStartResponse();
    return message;
  },
};

function createBaseContainerStopRequest(): ContainerStopRequest {
  return { containerId: "", timeoutSeconds: 0 };
}

export const ContainerStopRequest: MessageFns<ContainerStopRequest> = {
  encode(message: ContainerStopRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    if (message.timeoutSeconds !== 0) {
      writer.uint32(16).uint32(message.timeoutSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStopRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStopRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timeoutSeconds = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerStopRequest {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
      timeoutSeconds: isSet(object.timeoutSeconds)
        ? globalThis.Number(object.timeoutSeconds)
        : isSet(object.timeout_seconds)
        ? globalThis.Number(object.timeout_seconds)
        : 0,
    };
  },

  toJSON(message: ContainerStopRequest): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.timeoutSeconds !== 0) {
      obj.timeoutSeconds = Math.round(message.timeoutSeconds);
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerStopRequest>): ContainerStopRequest {
    return ContainerStopRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerStopRequest>): ContainerStopRequest {
    const message = createBaseContainerStopRequest();
    message.containerId = object.containerId ?? "";
    message.timeoutSeconds = object.timeoutSeconds ?? 0;
    return message;
  },
};

function createBaseContainerStopResponse(): ContainerStopResponse {
  return {};
}

export const ContainerStopResponse: MessageFns<ContainerStopResponse> = {
  encode(_: ContainerStopResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStopResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStopResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ContainerStopResponse {
    return {};
  },

  toJSON(_: ContainerStopResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ContainerStopResponse>): ContainerStopResponse {
    return ContainerStopResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ContainerStopResponse>): ContainerStopResponse {
    const message = createBaseContainerStopResponse();
    return message;
  },
};

function createBaseContainerRemoveRequest(): ContainerRemoveRequest {
  return { containerId: "", force: false, removeVolumes: false };
}

export const ContainerRemoveRequest: MessageFns<ContainerRemoveRequest> = {
  encode(message: ContainerRemoveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    if (message.force !== false) {
      writer.uint32(16).bool(message.force);
    }
    if (message.removeVolumes !== false) {
      writer.uint32(24).bool(message.removeVolumes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerRemoveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerRemoveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.force = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.removeVolumes = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerRemoveRequest {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
      force: isSet(object.force) ? globalThis.Boolean(object.force) : false,
      removeVolumes: isSet(object.removeVolumes)
        ? globalThis.Boolean(object.removeVolumes)
        : isSet(object.remove_volumes)
        ? globalThis.Boolean(object.remove_volumes)
        : false,
    };
  },

  toJSON(message: ContainerRemoveRequest): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.force !== false) {
      obj.force = message.force;
    }
    if (message.removeVolumes !== false) {
      obj.removeVolumes = message.removeVolumes;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerRemoveRequest>): ContainerRemoveRequest {
    return ContainerRemoveRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerRemoveRequest>): ContainerRemoveRequest {
    const message = createBaseContainerRemoveRequest();
    message.containerId = object.containerId ?? "";
    message.force = object.force ?? false;
    message.removeVolumes = object.removeVolumes ?? false;
    return message;
  },
};

function createBaseContainerRemoveResponse(): ContainerRemoveResponse {
  return {};
}

export const ContainerRemoveResponse: MessageFns<ContainerRemoveResponse> = {
  encode(_: ContainerRemoveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerRemoveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerRemoveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ContainerRemoveResponse {
    return {};
  },

  toJSON(_: ContainerRemoveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ContainerRemoveResponse>): ContainerRemoveResponse {
    return ContainerRemoveResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ContainerRemoveResponse>): ContainerRemoveResponse {
    const message = createBaseContainerRemoveResponse();
    return message;
  },
};

function createBaseContainerLogsRequest(): ContainerLogsRequest {
  return { containerId: "", follow: false, sinceUnix: 0, tail: 0 };
}

export const ContainerLogsRequest: MessageFns<ContainerLogsRequest> = {
  encode(message: ContainerLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    if (message.follow !== false) {
      writer.uint32(16).bool(message.follow);
    }
    if (message.sinceUnix !== 0) {
      writer.uint32(24).int64(message.sinceUnix);
    }
    if (message.tail !== 0) {
      writer.uint32(32).uint32(message.tail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.follow = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sinceUnix = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tail = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerLogsRequest {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
      follow: isSet(object.follow) ? globalThis.Boolean(object.follow) : false,
      sinceUnix: isSet(object.sinceUnix)
        ? globalThis.Number(object.sinceUnix)
        : isSet(object.since_unix)
        ? globalThis.Number(object.since_unix)
        : 0,
      tail: isSet(object.tail) ? globalThis.Number(object.tail) : 0,
    };
  },

  toJSON(message: ContainerLogsRequest): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.follow !== false) {
      obj.follow = message.follow;
    }
    if (message.sinceUnix !== 0) {
      obj.sinceUnix = Math.round(message.sinceUnix);
    }
    if (message.tail !== 0) {
      obj.tail = Math.round(message.tail);
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerLogsRequest>): ContainerLogsRequest {
    return ContainerLogsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerLogsRequest>): ContainerLogsRequest {
    const message = createBaseContainerLogsRequest();
    message.containerId = object.containerId ?? "";
    message.follow = object.follow ?? false;
    message.sinceUnix = object.sinceUnix ?? 0;
    message.tail = object.tail ?? 0;
    return message;
  },
};

function createBaseLogLine(): LogLine {
  return { stream: "", line: "", timestamp: "" };
}

export const LogLine: MessageFns<LogLine> = {
  encode(message: LogLine, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stream !== "") {
      writer.uint32(10).string(message.stream);
    }
    if (message.line !== "") {
      writer.uint32(18).string(message.line);
    }
    if (message.timestamp !== "") {
      writer.uint32(26).string(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogLine {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogLine();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stream = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.line = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timestamp = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogLine {
    return {
      stream: isSet(object.stream) ? globalThis.String(object.stream) : "",
      line: isSet(object.line) ? globalThis.String(object.line) : "",
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
    };
  },

  toJSON(message: LogLine): unknown {
    const obj: any = {};
    if (message.stream !== "") {
      obj.stream = message.stream;
    }
    if (message.line !== "") {
      obj.line = message.line;
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    return obj;
  },

  create(base?: DeepPartial<LogLine>): LogLine {
    return LogLine.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LogLine>): LogLine {
    const message = createBaseLogLine();
    message.stream = object.stream ?? "";
    message.line = object.line ?? "";
    message.timestamp = object.timestamp ?? "";
    return message;
  },
};

function createBaseContainerStatsRequest(): ContainerStatsRequest {
  return { containerId: "", stream: false };
}

export const ContainerStatsRequest: MessageFns<ContainerStatsRequest> = {
  encode(message: ContainerStatsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    if (message.stream !== false) {
      writer.uint32(16).bool(message.stream);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStatsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStatsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stream = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerStatsRequest {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
      stream: isSet(object.stream) ? globalThis.Boolean(object.stream) : false,
    };
  },

  toJSON(message: ContainerStatsRequest): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.stream !== false) {
      obj.stream = message.stream;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerStatsRequest>): ContainerStatsRequest {
    return ContainerStatsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerStatsRequest>): ContainerStatsRequest {
    const message = createBaseContainerStatsRequest();
    message.containerId = object.containerId ?? "";
    message.stream = object.stream ?? false;
    return message;
  },
};

function createBaseStatsFrame(): StatsFrame {
  return {
    containerId: "",
    timestampNs: 0,
    cpuPercent: 0,
    memoryBytes: 0,
    memoryLimitBytes: 0,
    netRxBytes: 0,
    netTxBytes: 0,
  };
}

export const StatsFrame: MessageFns<StatsFrame> = {
  encode(message: StatsFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    if (message.timestampNs !== 0) {
      writer.uint32(16).int64(message.timestampNs);
    }
    if (message.cpuPercent !== 0) {
      writer.uint32(25).double(message.cpuPercent);
    }
    if (message.memoryBytes !== 0) {
      writer.uint32(32).int64(message.memoryBytes);
    }
    if (message.memoryLimitBytes !== 0) {
      writer.uint32(40).int64(message.memoryLimitBytes);
    }
    if (message.netRxBytes !== 0) {
      writer.uint32(48).int64(message.netRxBytes);
    }
    if (message.netTxBytes !== 0) {
      writer.uint32(56).int64(message.netTxBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatsFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatsFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.timestampNs = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.cpuPercent = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.memoryBytes = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.memoryLimitBytes = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.netRxBytes = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.netTxBytes = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatsFrame {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
      timestampNs: isSet(object.timestampNs)
        ? globalThis.Number(object.timestampNs)
        : isSet(object.timestamp_ns)
        ? globalThis.Number(object.timestamp_ns)
        : 0,
      cpuPercent: isSet(object.cpuPercent)
        ? globalThis.Number(object.cpuPercent)
        : isSet(object.cpu_percent)
        ? globalThis.Number(object.cpu_percent)
        : 0,
      memoryBytes: isSet(object.memoryBytes)
        ? globalThis.Number(object.memoryBytes)
        : isSet(object.memory_bytes)
        ? globalThis.Number(object.memory_bytes)
        : 0,
      memoryLimitBytes: isSet(object.memoryLimitBytes)
        ? globalThis.Number(object.memoryLimitBytes)
        : isSet(object.memory_limit_bytes)
        ? globalThis.Number(object.memory_limit_bytes)
        : 0,
      netRxBytes: isSet(object.netRxBytes)
        ? globalThis.Number(object.netRxBytes)
        : isSet(object.net_rx_bytes)
        ? globalThis.Number(object.net_rx_bytes)
        : 0,
      netTxBytes: isSet(object.netTxBytes)
        ? globalThis.Number(object.netTxBytes)
        : isSet(object.net_tx_bytes)
        ? globalThis.Number(object.net_tx_bytes)
        : 0,
    };
  },

  toJSON(message: StatsFrame): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.timestampNs !== 0) {
      obj.timestampNs = Math.round(message.timestampNs);
    }
    if (message.cpuPercent !== 0) {
      obj.cpuPercent = message.cpuPercent;
    }
    if (message.memoryBytes !== 0) {
      obj.memoryBytes = Math.round(message.memoryBytes);
    }
    if (message.memoryLimitBytes !== 0) {
      obj.memoryLimitBytes = Math.round(message.memoryLimitBytes);
    }
    if (message.netRxBytes !== 0) {
      obj.netRxBytes = Math.round(message.netRxBytes);
    }
    if (message.netTxBytes !== 0) {
      obj.netTxBytes = Math.round(message.netTxBytes);
    }
    return obj;
  },

  create(base?: DeepPartial<StatsFrame>): StatsFrame {
    return StatsFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatsFrame>): StatsFrame {
    const message = createBaseStatsFrame();
    message.containerId = object.containerId ?? "";
    message.timestampNs = object.timestampNs ?? 0;
    message.cpuPercent = object.cpuPercent ?? 0;
    message.memoryBytes = object.memoryBytes ?? 0;
    message.memoryLimitBytes = object.memoryLimitBytes ?? 0;
    message.netRxBytes = object.netRxBytes ?? 0;
    message.netTxBytes = object.netTxBytes ?? 0;
    return message;
  },
};

function createBaseRegistryAuth(): RegistryAuth {
  return { username: "", password: "" };
}

export const RegistryAuth: MessageFns<RegistryAuth> = {
  encode(message: RegistryAuth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegistryAuth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegistryAuth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegistryAuth {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: RegistryAuth): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create(base?: DeepPartial<RegistryAuth>): RegistryAuth {
    return RegistryAuth.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegistryAuth>): RegistryAuth {
    const message = createBaseRegistryAuth();
    message.username = object.username ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseImagePullRequest(): ImagePullRequest {
  return { image: "", registryAuth: undefined };
}

export const ImagePullRequest: MessageFns<ImagePullRequest> = {
  encode(message: ImagePullRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.image !== "") {
      writer.uint32(10).string(message.image);
    }
    if (message.registryAuth !== undefined) {
      RegistryAuth.encode(message.registryAuth, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImagePullRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImagePullRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.registryAuth = RegistryAuth.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImagePullRequest {
    return {
      image: isSet(object.image) ? globalThis.String(object.image) : "",
      registryAuth: isSet(object.registryAuth)
        ? RegistryAuth.fromJSON(object.registryAuth)
        : isSet(object.registry_auth)
        ? RegistryAuth.fromJSON(object.registry_auth)
        : undefined,
    };
  },

  toJSON(message: ImagePullRequest): unknown {
    const obj: any = {};
    if (message.image !== "") {
      obj.image = message.image;
    }
    if (message.registryAuth !== undefined) {
      obj.registryAuth = RegistryAuth.toJSON(message.registryAuth);
    }
    return obj;
  },

  create(base?: DeepPartial<ImagePullRequest>): ImagePullRequest {
    return ImagePullRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImagePullRequest>): ImagePullRequest {
    const message = createBaseImagePullRequest();
    message.image = object.image ?? "";
    message.registryAuth = (object.registryAuth !== undefined && object.registryAuth !== null)
      ? RegistryAuth.fromPartial(object.registryAuth)
      : undefined;
    return message;
  },
};

function createBasePullProgress(): PullProgress {
  return { status: "", layerId: "", current: 0, total: 0 };
}

export const PullProgress: MessageFns<PullProgress> = {
  encode(message: PullProgress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.layerId !== "") {
      writer.uint32(18).string(message.layerId);
    }
    if (message.current !== 0) {
      writer.uint32(24).int64(message.current);
    }
    if (message.total !== 0) {
      writer.uint32(32).int64(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PullProgress {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePullProgress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.layerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.current = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.total = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PullProgress {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      layerId: isSet(object.layerId)
        ? globalThis.String(object.layerId)
        : isSet(object.layer_id)
        ? globalThis.String(object.layer_id)
        : "",
      current: isSet(object.current) ? globalThis.Number(object.current) : 0,
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: PullProgress): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.layerId !== "") {
      obj.layerId = message.layerId;
    }
    if (message.current !== 0) {
      obj.current = Math.round(message.current);
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create(base?: DeepPartial<PullProgress>): PullProgress {
    return PullProgress.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PullProgress>): PullProgress {
    const message = createBasePullProgress();
    message.status = object.status ?? "";
    message.layerId = object.layerId ?? "";
    message.current = object.current ?? 0;
    message.total = object.total ?? 0;
    return message;
  },
};

function createBaseImageBuildRequest(): ImageBuildRequest {
  return { tag: "", dockerfile: new Uint8Array(0), context: {}, buildArgs: {} };
}

export const ImageBuildRequest: MessageFns<ImageBuildRequest> = {
  encode(message: ImageBuildRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tag !== "") {
      writer.uint32(10).string(message.tag);
    }
    if (message.dockerfile.length !== 0) {
      writer.uint32(18).bytes(message.dockerfile);
    }
    globalThis.Object.entries(message.context).forEach(([key, value]: [string, Uint8Array]) => {
      ImageBuildRequest_ContextEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    globalThis.Object.entries(message.buildArgs).forEach(([key, value]: [string, string]) => {
      ImageBuildRequest_BuildArgsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageBuildRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageBuildRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tag = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dockerfile = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ImageBuildRequest_ContextEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.context[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = ImageBuildRequest_BuildArgsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.buildArgs[entry4.key] = entry4.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageBuildRequest {
    return {
      tag: isSet(object.tag) ? globalThis.String(object.tag) : "",
      dockerfile: isSet(object.dockerfile) ? bytesFromBase64(object.dockerfile) : new Uint8Array(0),
      context: isObject(object.context)
        ? (globalThis.Object.entries(object.context) as [string, any][]).reduce(
          (acc: { [key: string]: Uint8Array }, [key, value]: [string, any]) => {
            acc[key] = bytesFromBase64(value as string);
            return acc;
          },
          {},
        )
        : {},
      buildArgs: isObject(object.buildArgs)
        ? (globalThis.Object.entries(object.buildArgs) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.build_args)
        ? (globalThis.Object.entries(object.build_args) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ImageBuildRequest): unknown {
    const obj: any = {};
    if (message.tag !== "") {
      obj.tag = message.tag;
    }
    if (message.dockerfile.length !== 0) {
      obj.dockerfile = base64FromBytes(message.dockerfile);
    }
    if (message.context) {
      const entries = globalThis.Object.entries(message.context) as [string, Uint8Array][];
      if (entries.length > 0) {
        obj.context = {};
        entries.forEach(([k, v]) => {
          obj.context[k] = base64FromBytes(v);
        });
      }
    }
    if (message.buildArgs) {
      const entries = globalThis.Object.entries(message.buildArgs) as [string, string][];
      if (entries.length > 0) {
        obj.buildArgs = {};
        entries.forEach(([k, v]) => {
          obj.buildArgs[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<ImageBuildRequest>): ImageBuildRequest {
    return ImageBuildRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageBuildRequest>): ImageBuildRequest {
    const message = createBaseImageBuildRequest();
    message.tag = object.tag ?? "";
    message.dockerfile = object.dockerfile ?? new Uint8Array(0);
    message.context = (globalThis.Object.entries(object.context ?? {}) as [string, Uint8Array][]).reduce(
      (acc: { [key: string]: Uint8Array }, [key, value]: [string, Uint8Array]) => {
        if (value !== undefined) {
          acc[key] = value;
        }
        return acc;
      },
      {},
    );
    message.buildArgs = (globalThis.Object.entries(object.buildArgs ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseImageBuildRequest_ContextEntry(): ImageBuildRequest_ContextEntry {
  return { key: "", value: new Uint8Array(0) };
}

export const ImageBuildRequest_ContextEntry: MessageFns<ImageBuildRequest_ContextEntry> = {
  encode(message: ImageBuildRequest_ContextEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageBuildRequest_ContextEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageBuildRequest_ContextEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageBuildRequest_ContextEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? bytesFromBase64(object.value) : new Uint8Array(0),
    };
  },

  toJSON(message: ImageBuildRequest_ContextEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<ImageBuildRequest_ContextEntry>): ImageBuildRequest_ContextEntry {
    return ImageBuildRequest_ContextEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageBuildRequest_ContextEntry>): ImageBuildRequest_ContextEntry {
    const message = createBaseImageBuildRequest_ContextEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? new Uint8Array(0);
    return message;
  },
};

function createBaseImageBuildRequest_BuildArgsEntry(): ImageBuildRequest_BuildArgsEntry {
  return { key: "", value: "" };
}

export const ImageBuildRequest_BuildArgsEntry: MessageFns<ImageBuildRequest_BuildArgsEntry> = {
  encode(message: ImageBuildRequest_BuildArgsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImageBuildRequest_BuildArgsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImageBuildRequest_BuildArgsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImageBuildRequest_BuildArgsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ImageBuildRequest_BuildArgsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<ImageBuildRequest_BuildArgsEntry>): ImageBuildRequest_BuildArgsEntry {
    return ImageBuildRequest_BuildArgsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImageBuildRequest_BuildArgsEntry>): ImageBuildRequest_BuildArgsEntry {
    const message = createBaseImageBuildRequest_BuildArgsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseBuildProgress(): BuildProgress {
  return { stream: "", error: "" };
}

export const BuildProgress: MessageFns<BuildProgress> = {
  encode(message: BuildProgress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stream !== "") {
      writer.uint32(10).string(message.stream);
    }
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildProgress {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildProgress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stream = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildProgress {
    return {
      stream: isSet(object.stream) ? globalThis.String(object.stream) : "",
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: BuildProgress): unknown {
    const obj: any = {};
    if (message.stream !== "") {
      obj.stream = message.stream;
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<BuildProgress>): BuildProgress {
    return BuildProgress.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BuildProgress>): BuildProgress {
    const message = createBaseBuildProgress();
    message.stream = object.stream ?? "";
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseNetworkCreateRequest(): NetworkCreateRequest {
  return { name: "", driver: "", labels: {} };
}

export const NetworkCreateRequest: MessageFns<NetworkCreateRequest> = {
  encode(message: NetworkCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.driver !== "") {
      writer.uint32(18).string(message.driver);
    }
    globalThis.Object.entries(message.labels).forEach(([key, value]: [string, string]) => {
      NetworkCreateRequest_LabelsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.driver = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = NetworkCreateRequest_LabelsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.labels[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkCreateRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      driver: isSet(object.driver) ? globalThis.String(object.driver) : "",
      labels: isObject(object.labels)
        ? (globalThis.Object.entries(object.labels) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: NetworkCreateRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.driver !== "") {
      obj.driver = message.driver;
    }
    if (message.labels) {
      const entries = globalThis.Object.entries(message.labels) as [string, string][];
      if (entries.length > 0) {
        obj.labels = {};
        entries.forEach(([k, v]) => {
          obj.labels[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<NetworkCreateRequest>): NetworkCreateRequest {
    return NetworkCreateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NetworkCreateRequest>): NetworkCreateRequest {
    const message = createBaseNetworkCreateRequest();
    message.name = object.name ?? "";
    message.driver = object.driver ?? "";
    message.labels = (globalThis.Object.entries(object.labels ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseNetworkCreateRequest_LabelsEntry(): NetworkCreateRequest_LabelsEntry {
  return { key: "", value: "" };
}

export const NetworkCreateRequest_LabelsEntry: MessageFns<NetworkCreateRequest_LabelsEntry> = {
  encode(message: NetworkCreateRequest_LabelsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkCreateRequest_LabelsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkCreateRequest_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkCreateRequest_LabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: NetworkCreateRequest_LabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<NetworkCreateRequest_LabelsEntry>): NetworkCreateRequest_LabelsEntry {
    return NetworkCreateRequest_LabelsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NetworkCreateRequest_LabelsEntry>): NetworkCreateRequest_LabelsEntry {
    const message = createBaseNetworkCreateRequest_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseNetworkCreateResponse(): NetworkCreateResponse {
  return { networkId: "" };
}

export const NetworkCreateResponse: MessageFns<NetworkCreateResponse> = {
  encode(message: NetworkCreateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.networkId !== "") {
      writer.uint32(10).string(message.networkId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkCreateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkCreateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.networkId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkCreateResponse {
    return {
      networkId: isSet(object.networkId)
        ? globalThis.String(object.networkId)
        : isSet(object.network_id)
        ? globalThis.String(object.network_id)
        : "",
    };
  },

  toJSON(message: NetworkCreateResponse): unknown {
    const obj: any = {};
    if (message.networkId !== "") {
      obj.networkId = message.networkId;
    }
    return obj;
  },

  create(base?: DeepPartial<NetworkCreateResponse>): NetworkCreateResponse {
    return NetworkCreateResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NetworkCreateResponse>): NetworkCreateResponse {
    const message = createBaseNetworkCreateResponse();
    message.networkId = object.networkId ?? "";
    return message;
  },
};

function createBaseNetworkRemoveRequest(): NetworkRemoveRequest {
  return { networkId: "" };
}

export const NetworkRemoveRequest: MessageFns<NetworkRemoveRequest> = {
  encode(message: NetworkRemoveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.networkId !== "") {
      writer.uint32(10).string(message.networkId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkRemoveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkRemoveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.networkId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkRemoveRequest {
    return {
      networkId: isSet(object.networkId)
        ? globalThis.String(object.networkId)
        : isSet(object.network_id)
        ? globalThis.String(object.network_id)
        : "",
    };
  },

  toJSON(message: NetworkRemoveRequest): unknown {
    const obj: any = {};
    if (message.networkId !== "") {
      obj.networkId = message.networkId;
    }
    return obj;
  },

  create(base?: DeepPartial<NetworkRemoveRequest>): NetworkRemoveRequest {
    return NetworkRemoveRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NetworkRemoveRequest>): NetworkRemoveRequest {
    const message = createBaseNetworkRemoveRequest();
    message.networkId = object.networkId ?? "";
    return message;
  },
};

function createBaseNetworkRemoveResponse(): NetworkRemoveResponse {
  return {};
}

export const NetworkRemoveResponse: MessageFns<NetworkRemoveResponse> = {
  encode(_: NetworkRemoveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkRemoveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkRemoveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): NetworkRemoveResponse {
    return {};
  },

  toJSON(_: NetworkRemoveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<NetworkRemoveResponse>): NetworkRemoveResponse {
    return NetworkRemoveResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<NetworkRemoveResponse>): NetworkRemoveResponse {
    const message = createBaseNetworkRemoveResponse();
    return message;
  },
};

function createBaseContainerSnapshot(): ContainerSnapshot {
  return {
    id: "",
    name: "",
    image: "",
    status: "",
    uptimeS: 0,
    cpuPct: 0,
    memBytes: 0,
    memLimitBytes: 0,
    restartCount: 0,
    kind: "",
    appId: "",
    color: "",
    lastPingMs: 0,
    lastPingOk: false,
    lastSeenMs: 0,
  };
}

export const ContainerSnapshot: MessageFns<ContainerSnapshot> = {
  encode(message: ContainerSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.image !== "") {
      writer.uint32(26).string(message.image);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    if (message.uptimeS !== 0) {
      writer.uint32(40).uint64(message.uptimeS);
    }
    if (message.cpuPct !== 0) {
      writer.uint32(49).double(message.cpuPct);
    }
    if (message.memBytes !== 0) {
      writer.uint32(56).uint64(message.memBytes);
    }
    if (message.memLimitBytes !== 0) {
      writer.uint32(64).uint64(message.memLimitBytes);
    }
    if (message.restartCount !== 0) {
      writer.uint32(72).uint32(message.restartCount);
    }
    if (message.kind !== "") {
      writer.uint32(82).string(message.kind);
    }
    if (message.appId !== "") {
      writer.uint32(90).string(message.appId);
    }
    if (message.color !== "") {
      writer.uint32(98).string(message.color);
    }
    if (message.lastPingMs !== 0) {
      writer.uint32(104).uint64(message.lastPingMs);
    }
    if (message.lastPingOk !== false) {
      writer.uint32(112).bool(message.lastPingOk);
    }
    if (message.lastSeenMs !== 0) {
      writer.uint32(120).uint64(message.lastSeenMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.uptimeS = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.cpuPct = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.memBytes = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.memLimitBytes = longToNumber(reader.uint64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.restartCount = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.appId = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.color = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.lastPingMs = longToNumber(reader.uint64());
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.lastPingOk = reader.bool();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.lastSeenMs = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerSnapshot {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      image: isSet(object.image) ? globalThis.String(object.image) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      uptimeS: isSet(object.uptimeS)
        ? globalThis.Number(object.uptimeS)
        : isSet(object.uptime_s)
        ? globalThis.Number(object.uptime_s)
        : 0,
      cpuPct: isSet(object.cpuPct)
        ? globalThis.Number(object.cpuPct)
        : isSet(object.cpu_pct)
        ? globalThis.Number(object.cpu_pct)
        : 0,
      memBytes: isSet(object.memBytes)
        ? globalThis.Number(object.memBytes)
        : isSet(object.mem_bytes)
        ? globalThis.Number(object.mem_bytes)
        : 0,
      memLimitBytes: isSet(object.memLimitBytes)
        ? globalThis.Number(object.memLimitBytes)
        : isSet(object.mem_limit_bytes)
        ? globalThis.Number(object.mem_limit_bytes)
        : 0,
      restartCount: isSet(object.restartCount)
        ? globalThis.Number(object.restartCount)
        : isSet(object.restart_count)
        ? globalThis.Number(object.restart_count)
        : 0,
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      appId: isSet(object.appId)
        ? globalThis.String(object.appId)
        : isSet(object.app_id)
        ? globalThis.String(object.app_id)
        : "",
      color: isSet(object.color) ? globalThis.String(object.color) : "",
      lastPingMs: isSet(object.lastPingMs)
        ? globalThis.Number(object.lastPingMs)
        : isSet(object.last_ping_ms)
        ? globalThis.Number(object.last_ping_ms)
        : 0,
      lastPingOk: isSet(object.lastPingOk)
        ? globalThis.Boolean(object.lastPingOk)
        : isSet(object.last_ping_ok)
        ? globalThis.Boolean(object.last_ping_ok)
        : false,
      lastSeenMs: isSet(object.lastSeenMs)
        ? globalThis.Number(object.lastSeenMs)
        : isSet(object.last_seen_ms)
        ? globalThis.Number(object.last_seen_ms)
        : 0,
    };
  },

  toJSON(message: ContainerSnapshot): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.image !== "") {
      obj.image = message.image;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.uptimeS !== 0) {
      obj.uptimeS = Math.round(message.uptimeS);
    }
    if (message.cpuPct !== 0) {
      obj.cpuPct = message.cpuPct;
    }
    if (message.memBytes !== 0) {
      obj.memBytes = Math.round(message.memBytes);
    }
    if (message.memLimitBytes !== 0) {
      obj.memLimitBytes = Math.round(message.memLimitBytes);
    }
    if (message.restartCount !== 0) {
      obj.restartCount = Math.round(message.restartCount);
    }
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.appId !== "") {
      obj.appId = message.appId;
    }
    if (message.color !== "") {
      obj.color = message.color;
    }
    if (message.lastPingMs !== 0) {
      obj.lastPingMs = Math.round(message.lastPingMs);
    }
    if (message.lastPingOk !== false) {
      obj.lastPingOk = message.lastPingOk;
    }
    if (message.lastSeenMs !== 0) {
      obj.lastSeenMs = Math.round(message.lastSeenMs);
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerSnapshot>): ContainerSnapshot {
    return ContainerSnapshot.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerSnapshot>): ContainerSnapshot {
    const message = createBaseContainerSnapshot();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.image = object.image ?? "";
    message.status = object.status ?? "";
    message.uptimeS = object.uptimeS ?? 0;
    message.cpuPct = object.cpuPct ?? 0;
    message.memBytes = object.memBytes ?? 0;
    message.memLimitBytes = object.memLimitBytes ?? 0;
    message.restartCount = object.restartCount ?? 0;
    message.kind = object.kind ?? "";
    message.appId = object.appId ?? "";
    message.color = object.color ?? "";
    message.lastPingMs = object.lastPingMs ?? 0;
    message.lastPingOk = object.lastPingOk ?? false;
    message.lastSeenMs = object.lastSeenMs ?? 0;
    return message;
  },
};

function createBaseListContainersRequest(): ListContainersRequest {
  return { kindFilter: "" };
}

export const ListContainersRequest: MessageFns<ListContainersRequest> = {
  encode(message: ListContainersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kindFilter !== "") {
      writer.uint32(10).string(message.kindFilter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListContainersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListContainersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kindFilter = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListContainersRequest {
    return {
      kindFilter: isSet(object.kindFilter)
        ? globalThis.String(object.kindFilter)
        : isSet(object.kind_filter)
        ? globalThis.String(object.kind_filter)
        : "",
    };
  },

  toJSON(message: ListContainersRequest): unknown {
    const obj: any = {};
    if (message.kindFilter !== "") {
      obj.kindFilter = message.kindFilter;
    }
    return obj;
  },

  create(base?: DeepPartial<ListContainersRequest>): ListContainersRequest {
    return ListContainersRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListContainersRequest>): ListContainersRequest {
    const message = createBaseListContainersRequest();
    message.kindFilter = object.kindFilter ?? "";
    return message;
  },
};

function createBaseListContainersResponse(): ListContainersResponse {
  return { containers: [] };
}

export const ListContainersResponse: MessageFns<ListContainersResponse> = {
  encode(message: ListContainersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.containers) {
      ContainerSnapshot.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListContainersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListContainersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containers.push(ContainerSnapshot.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListContainersResponse {
    return {
      containers: globalThis.Array.isArray(object?.containers)
        ? object.containers.map((e: any) => ContainerSnapshot.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListContainersResponse): unknown {
    const obj: any = {};
    if (message.containers?.length) {
      obj.containers = message.containers.map((e) => ContainerSnapshot.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListContainersResponse>): ListContainersResponse {
    return ListContainersResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListContainersResponse>): ListContainersResponse {
    const message = createBaseListContainersResponse();
    message.containers = object.containers?.map((e) => ContainerSnapshot.fromPartial(e)) || [];
    return message;
  },
};

function createBasePingContainerRequest(): PingContainerRequest {
  return { containerId: "", path: "", port: 0, timeoutMs: 0 };
}

export const PingContainerRequest: MessageFns<PingContainerRequest> = {
  encode(message: PingContainerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    if (message.path !== "") {
      writer.uint32(18).string(message.path);
    }
    if (message.port !== 0) {
      writer.uint32(24).uint32(message.port);
    }
    if (message.timeoutMs !== 0) {
      writer.uint32(32).uint32(message.timeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingContainerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingContainerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timeoutMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingContainerRequest {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
      path: isSet(object.path) ? globalThis.String(object.path) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      timeoutMs: isSet(object.timeoutMs)
        ? globalThis.Number(object.timeoutMs)
        : isSet(object.timeout_ms)
        ? globalThis.Number(object.timeout_ms)
        : 0,
    };
  },

  toJSON(message: PingContainerRequest): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.path !== "") {
      obj.path = message.path;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.timeoutMs !== 0) {
      obj.timeoutMs = Math.round(message.timeoutMs);
    }
    return obj;
  },

  create(base?: DeepPartial<PingContainerRequest>): PingContainerRequest {
    return PingContainerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PingContainerRequest>): PingContainerRequest {
    const message = createBasePingContainerRequest();
    message.containerId = object.containerId ?? "";
    message.path = object.path ?? "";
    message.port = object.port ?? 0;
    message.timeoutMs = object.timeoutMs ?? 0;
    return message;
  },
};

function createBasePingContainerResponse(): PingContainerResponse {
  return { ok: false, statusCode: 0, latencyMs: 0, error: "" };
}

export const PingContainerResponse: MessageFns<PingContainerResponse> = {
  encode(message: PingContainerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    if (message.statusCode !== 0) {
      writer.uint32(16).uint32(message.statusCode);
    }
    if (message.latencyMs !== 0) {
      writer.uint32(24).uint64(message.latencyMs);
    }
    if (message.error !== "") {
      writer.uint32(34).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PingContainerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePingContainerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.statusCode = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.latencyMs = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PingContainerResponse {
    return {
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      statusCode: isSet(object.statusCode)
        ? globalThis.Number(object.statusCode)
        : isSet(object.status_code)
        ? globalThis.Number(object.status_code)
        : 0,
      latencyMs: isSet(object.latencyMs)
        ? globalThis.Number(object.latencyMs)
        : isSet(object.latency_ms)
        ? globalThis.Number(object.latency_ms)
        : 0,
      error: isSet(object.error) ? globalThis.String(object.error) : "",
    };
  },

  toJSON(message: PingContainerResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.statusCode !== 0) {
      obj.statusCode = Math.round(message.statusCode);
    }
    if (message.latencyMs !== 0) {
      obj.latencyMs = Math.round(message.latencyMs);
    }
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<PingContainerResponse>): PingContainerResponse {
    return PingContainerResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PingContainerResponse>): PingContainerResponse {
    const message = createBasePingContainerResponse();
    message.ok = object.ok ?? false;
    message.statusCode = object.statusCode ?? 0;
    message.latencyMs = object.latencyMs ?? 0;
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseExecFrame(): ExecFrame {
  return {
    start: undefined,
    stdin: undefined,
    stdout: undefined,
    stderr: undefined,
    resize: undefined,
    exit: undefined,
    ready: undefined,
  };
}

export const ExecFrame: MessageFns<ExecFrame> = {
  encode(message: ExecFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== undefined) {
      ExecStart.encode(message.start, writer.uint32(10).fork()).join();
    }
    if (message.stdin !== undefined) {
      writer.uint32(18).bytes(message.stdin);
    }
    if (message.stdout !== undefined) {
      writer.uint32(26).bytes(message.stdout);
    }
    if (message.stderr !== undefined) {
      writer.uint32(34).bytes(message.stderr);
    }
    if (message.resize !== undefined) {
      ExecResize.encode(message.resize, writer.uint32(42).fork()).join();
    }
    if (message.exit !== undefined) {
      ExecExit.encode(message.exit, writer.uint32(50).fork()).join();
    }
    if (message.ready !== undefined) {
      ExecReady.encode(message.ready, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start = ExecStart.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.stdin = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.stdout = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.stderr = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.resize = ExecResize.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.exit = ExecExit.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.ready = ExecReady.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecFrame {
    return {
      start: isSet(object.start) ? ExecStart.fromJSON(object.start) : undefined,
      stdin: isSet(object.stdin) ? bytesFromBase64(object.stdin) : undefined,
      stdout: isSet(object.stdout) ? bytesFromBase64(object.stdout) : undefined,
      stderr: isSet(object.stderr) ? bytesFromBase64(object.stderr) : undefined,
      resize: isSet(object.resize) ? ExecResize.fromJSON(object.resize) : undefined,
      exit: isSet(object.exit) ? ExecExit.fromJSON(object.exit) : undefined,
      ready: isSet(object.ready) ? ExecReady.fromJSON(object.ready) : undefined,
    };
  },

  toJSON(message: ExecFrame): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = ExecStart.toJSON(message.start);
    }
    if (message.stdin !== undefined) {
      obj.stdin = base64FromBytes(message.stdin);
    }
    if (message.stdout !== undefined) {
      obj.stdout = base64FromBytes(message.stdout);
    }
    if (message.stderr !== undefined) {
      obj.stderr = base64FromBytes(message.stderr);
    }
    if (message.resize !== undefined) {
      obj.resize = ExecResize.toJSON(message.resize);
    }
    if (message.exit !== undefined) {
      obj.exit = ExecExit.toJSON(message.exit);
    }
    if (message.ready !== undefined) {
      obj.ready = ExecReady.toJSON(message.ready);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecFrame>): ExecFrame {
    return ExecFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecFrame>): ExecFrame {
    const message = createBaseExecFrame();
    message.start = (object.start !== undefined && object.start !== null)
      ? ExecStart.fromPartial(object.start)
      : undefined;
    message.stdin = object.stdin ?? undefined;
    message.stdout = object.stdout ?? undefined;
    message.stderr = object.stderr ?? undefined;
    message.resize = (object.resize !== undefined && object.resize !== null)
      ? ExecResize.fromPartial(object.resize)
      : undefined;
    message.exit = (object.exit !== undefined && object.exit !== null) ? ExecExit.fromPartial(object.exit) : undefined;
    message.ready = (object.ready !== undefined && object.ready !== null)
      ? ExecReady.fromPartial(object.ready)
      : undefined;
    return message;
  },
};

function createBaseExecStart(): ExecStart {
  return { containerId: "", cmd: [], tty: false, cols: 0, rows: 0, user: "" };
}

export const ExecStart: MessageFns<ExecStart> = {
  encode(message: ExecStart, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containerId !== "") {
      writer.uint32(10).string(message.containerId);
    }
    for (const v of message.cmd) {
      writer.uint32(18).string(v!);
    }
    if (message.tty !== false) {
      writer.uint32(24).bool(message.tty);
    }
    if (message.cols !== 0) {
      writer.uint32(32).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(40).uint32(message.rows);
    }
    if (message.user !== "") {
      writer.uint32(50).string(message.user);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecStart {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecStart();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cmd.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.tty = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.user = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecStart {
    return {
      containerId: isSet(object.containerId)
        ? globalThis.String(object.containerId)
        : isSet(object.container_id)
        ? globalThis.String(object.container_id)
        : "",
      cmd: globalThis.Array.isArray(object?.cmd) ? object.cmd.map((e: any) => globalThis.String(e)) : [],
      tty: isSet(object.tty) ? globalThis.Boolean(object.tty) : false,
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
      user: isSet(object.user) ? globalThis.String(object.user) : "",
    };
  },

  toJSON(message: ExecStart): unknown {
    const obj: any = {};
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.cmd?.length) {
      obj.cmd = message.cmd;
    }
    if (message.tty !== false) {
      obj.tty = message.tty;
    }
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    if (message.user !== "") {
      obj.user = message.user;
    }
    return obj;
  },

  create(base?: DeepPartial<ExecStart>): ExecStart {
    return ExecStart.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecStart>): ExecStart {
    const message = createBaseExecStart();
    message.containerId = object.containerId ?? "";
    message.cmd = object.cmd?.map((e) => e) || [];
    message.tty = object.tty ?? false;
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    message.user = object.user ?? "";
    return message;
  },
};

function createBaseExecResize(): ExecResize {
  return { cols: 0, rows: 0 };
}

export const ExecResize: MessageFns<ExecResize> = {
  encode(message: ExecResize, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cols !== 0) {
      writer.uint32(8).uint32(message.cols);
    }
    if (message.rows !== 0) {
      writer.uint32(16).uint32(message.rows);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecResize {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecResize();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.cols = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rows = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecResize {
    return {
      cols: isSet(object.cols) ? globalThis.Number(object.cols) : 0,
      rows: isSet(object.rows) ? globalThis.Number(object.rows) : 0,
    };
  },

  toJSON(message: ExecResize): unknown {
    const obj: any = {};
    if (message.cols !== 0) {
      obj.cols = Math.round(message.cols);
    }
    if (message.rows !== 0) {
      obj.rows = Math.round(message.rows);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecResize>): ExecResize {
    return ExecResize.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecResize>): ExecResize {
    const message = createBaseExecResize();
    message.cols = object.cols ?? 0;
    message.rows = object.rows ?? 0;
    return message;
  },
};

function createBaseExecExit(): ExecExit {
  return { code: 0 };
}

export const ExecExit: MessageFns<ExecExit> = {
  encode(message: ExecExit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== 0) {
      writer.uint32(8).int32(message.code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecExit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecExit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecExit {
    return { code: isSet(object.code) ? globalThis.Number(object.code) : 0 };
  },

  toJSON(message: ExecExit): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = Math.round(message.code);
    }
    return obj;
  },

  create(base?: DeepPartial<ExecExit>): ExecExit {
    return ExecExit.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecExit>): ExecExit {
    const message = createBaseExecExit();
    message.code = object.code ?? 0;
    return message;
  },
};

function createBaseExecReady(): ExecReady {
  return { execId: "" };
}

export const ExecReady: MessageFns<ExecReady> = {
  encode(message: ExecReady, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.execId !== "") {
      writer.uint32(10).string(message.execId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ExecReady {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExecReady();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.execId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExecReady {
    return {
      execId: isSet(object.execId)
        ? globalThis.String(object.execId)
        : isSet(object.exec_id)
        ? globalThis.String(object.exec_id)
        : "",
    };
  },

  toJSON(message: ExecReady): unknown {
    const obj: any = {};
    if (message.execId !== "") {
      obj.execId = message.execId;
    }
    return obj;
  },

  create(base?: DeepPartial<ExecReady>): ExecReady {
    return ExecReady.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ExecReady>): ExecReady {
    const message = createBaseExecReady();
    message.execId = object.execId ?? "";
    return message;
  },
};

export type AgentService = typeof AgentService;
export const AgentService = {
  /** Container lifecycle */
  containerCreate: {
    path: "/ploydok.agent.v1.Agent/ContainerCreate" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ContainerCreateRequest): Buffer =>
      Buffer.from(ContainerCreateRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContainerCreateRequest => ContainerCreateRequest.decode(value),
    responseSerialize: (value: ContainerCreateResponse): Buffer =>
      Buffer.from(ContainerCreateResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ContainerCreateResponse => ContainerCreateResponse.decode(value),
  },
  containerStart: {
    path: "/ploydok.agent.v1.Agent/ContainerStart" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ContainerStartRequest): Buffer =>
      Buffer.from(ContainerStartRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContainerStartRequest => ContainerStartRequest.decode(value),
    responseSerialize: (value: ContainerStartResponse): Buffer =>
      Buffer.from(ContainerStartResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ContainerStartResponse => ContainerStartResponse.decode(value),
  },
  containerStop: {
    path: "/ploydok.agent.v1.Agent/ContainerStop" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ContainerStopRequest): Buffer => Buffer.from(ContainerStopRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContainerStopRequest => ContainerStopRequest.decode(value),
    responseSerialize: (value: ContainerStopResponse): Buffer =>
      Buffer.from(ContainerStopResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ContainerStopResponse => ContainerStopResponse.decode(value),
  },
  containerRemove: {
    path: "/ploydok.agent.v1.Agent/ContainerRemove" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ContainerRemoveRequest): Buffer =>
      Buffer.from(ContainerRemoveRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContainerRemoveRequest => ContainerRemoveRequest.decode(value),
    responseSerialize: (value: ContainerRemoveResponse): Buffer =>
      Buffer.from(ContainerRemoveResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ContainerRemoveResponse => ContainerRemoveResponse.decode(value),
  },
  /** Observability (streaming) */
  containerLogs: {
    path: "/ploydok.agent.v1.Agent/ContainerLogs" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: ContainerLogsRequest): Buffer => Buffer.from(ContainerLogsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContainerLogsRequest => ContainerLogsRequest.decode(value),
    responseSerialize: (value: LogLine): Buffer => Buffer.from(LogLine.encode(value).finish()),
    responseDeserialize: (value: Buffer): LogLine => LogLine.decode(value),
  },
  containerStats: {
    path: "/ploydok.agent.v1.Agent/ContainerStats" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: ContainerStatsRequest): Buffer =>
      Buffer.from(ContainerStatsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContainerStatsRequest => ContainerStatsRequest.decode(value),
    responseSerialize: (value: StatsFrame): Buffer => Buffer.from(StatsFrame.encode(value).finish()),
    responseDeserialize: (value: Buffer): StatsFrame => StatsFrame.decode(value),
  },
  /** Image management (streaming progress) */
  imagePull: {
    path: "/ploydok.agent.v1.Agent/ImagePull" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: ImagePullRequest): Buffer => Buffer.from(ImagePullRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ImagePullRequest => ImagePullRequest.decode(value),
    responseSerialize: (value: PullProgress): Buffer => Buffer.from(PullProgress.encode(value).finish()),
    responseDeserialize: (value: Buffer): PullProgress => PullProgress.decode(value),
  },
  imageBuild: {
    path: "/ploydok.agent.v1.Agent/ImageBuild" as const,
    requestStream: false as const,
    responseStream: true as const,
    requestSerialize: (value: ImageBuildRequest): Buffer => Buffer.from(ImageBuildRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ImageBuildRequest => ImageBuildRequest.decode(value),
    responseSerialize: (value: BuildProgress): Buffer => Buffer.from(BuildProgress.encode(value).finish()),
    responseDeserialize: (value: Buffer): BuildProgress => BuildProgress.decode(value),
  },
  /** Networking */
  networkCreate: {
    path: "/ploydok.agent.v1.Agent/NetworkCreate" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: NetworkCreateRequest): Buffer => Buffer.from(NetworkCreateRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): NetworkCreateRequest => NetworkCreateRequest.decode(value),
    responseSerialize: (value: NetworkCreateResponse): Buffer =>
      Buffer.from(NetworkCreateResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): NetworkCreateResponse => NetworkCreateResponse.decode(value),
  },
  networkRemove: {
    path: "/ploydok.agent.v1.Agent/NetworkRemove" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: NetworkRemoveRequest): Buffer => Buffer.from(NetworkRemoveRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): NetworkRemoveRequest => NetworkRemoveRequest.decode(value),
    responseSerialize: (value: NetworkRemoveResponse): Buffer =>
      Buffer.from(NetworkRemoveResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): NetworkRemoveResponse => NetworkRemoveResponse.decode(value),
  },
  /** Monitoring (snapshot + ad-hoc ping) */
  listContainers: {
    path: "/ploydok.agent.v1.Agent/ListContainers" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListContainersRequest): Buffer =>
      Buffer.from(ListContainersRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListContainersRequest => ListContainersRequest.decode(value),
    responseSerialize: (value: ListContainersResponse): Buffer =>
      Buffer.from(ListContainersResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListContainersResponse => ListContainersResponse.decode(value),
  },
  pingContainer: {
    path: "/ploydok.agent.v1.Agent/PingContainer" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PingContainerRequest): Buffer => Buffer.from(PingContainerRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PingContainerRequest => PingContainerRequest.decode(value),
    responseSerialize: (value: PingContainerResponse): Buffer =>
      Buffer.from(PingContainerResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PingContainerResponse => PingContainerResponse.decode(value),
  },
  /** Interactive shell (bidi streaming) */
  containerExec: {
    path: "/ploydok.agent.v1.Agent/ContainerExec" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: ExecFrame): Buffer => Buffer.from(ExecFrame.encode(value).finish()),
    requestDeserialize: (value: Buffer): ExecFrame => ExecFrame.decode(value),
    responseSerialize: (value: ExecFrame): Buffer => Buffer.from(ExecFrame.encode(value).finish()),
    responseDeserialize: (value: Buffer): ExecFrame => ExecFrame.decode(value),
  },
} as const;

export interface AgentServer extends UntypedServiceImplementation {
  /** Container lifecycle */
  containerCreate: handleUnaryCall<ContainerCreateRequest, ContainerCreateResponse>;
  containerStart: handleUnaryCall<ContainerStartRequest, ContainerStartResponse>;
  containerStop: handleUnaryCall<ContainerStopRequest, ContainerStopResponse>;
  containerRemove: handleUnaryCall<ContainerRemoveRequest, ContainerRemoveResponse>;
  /** Observability (streaming) */
  containerLogs: handleServerStreamingCall<ContainerLogsRequest, LogLine>;
  containerStats: handleServerStreamingCall<ContainerStatsRequest, StatsFrame>;
  /** Image management (streaming progress) */
  imagePull: handleServerStreamingCall<ImagePullRequest, PullProgress>;
  imageBuild: handleServerStreamingCall<ImageBuildRequest, BuildProgress>;
  /** Networking */
  networkCreate: handleUnaryCall<NetworkCreateRequest, NetworkCreateResponse>;
  networkRemove: handleUnaryCall<NetworkRemoveRequest, NetworkRemoveResponse>;
  /** Monitoring (snapshot + ad-hoc ping) */
  listContainers: handleUnaryCall<ListContainersRequest, ListContainersResponse>;
  pingContainer: handleUnaryCall<PingContainerRequest, PingContainerResponse>;
  /** Interactive shell (bidi streaming) */
  containerExec: handleBidiStreamingCall<ExecFrame, ExecFrame>;
}

export interface AgentClient extends Client {
  /** Container lifecycle */
  containerCreate(
    request: ContainerCreateRequest,
    callback: (error: ServiceError | null, response: ContainerCreateResponse) => void,
  ): ClientUnaryCall;
  containerCreate(
    request: ContainerCreateRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ContainerCreateResponse) => void,
  ): ClientUnaryCall;
  containerCreate(
    request: ContainerCreateRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ContainerCreateResponse) => void,
  ): ClientUnaryCall;
  containerStart(
    request: ContainerStartRequest,
    callback: (error: ServiceError | null, response: ContainerStartResponse) => void,
  ): ClientUnaryCall;
  containerStart(
    request: ContainerStartRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ContainerStartResponse) => void,
  ): ClientUnaryCall;
  containerStart(
    request: ContainerStartRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ContainerStartResponse) => void,
  ): ClientUnaryCall;
  containerStop(
    request: ContainerStopRequest,
    callback: (error: ServiceError | null, response: ContainerStopResponse) => void,
  ): ClientUnaryCall;
  containerStop(
    request: ContainerStopRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ContainerStopResponse) => void,
  ): ClientUnaryCall;
  containerStop(
    request: ContainerStopRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ContainerStopResponse) => void,
  ): ClientUnaryCall;
  containerRemove(
    request: ContainerRemoveRequest,
    callback: (error: ServiceError | null, response: ContainerRemoveResponse) => void,
  ): ClientUnaryCall;
  containerRemove(
    request: ContainerRemoveRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ContainerRemoveResponse) => void,
  ): ClientUnaryCall;
  containerRemove(
    request: ContainerRemoveRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ContainerRemoveResponse) => void,
  ): ClientUnaryCall;
  /** Observability (streaming) */
  containerLogs(request: ContainerLogsRequest, options?: Partial<CallOptions>): ClientReadableStream<LogLine>;
  containerLogs(
    request: ContainerLogsRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<LogLine>;
  containerStats(request: ContainerStatsRequest, options?: Partial<CallOptions>): ClientReadableStream<StatsFrame>;
  containerStats(
    request: ContainerStatsRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<StatsFrame>;
  /** Image management (streaming progress) */
  imagePull(request: ImagePullRequest, options?: Partial<CallOptions>): ClientReadableStream<PullProgress>;
  imagePull(
    request: ImagePullRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<PullProgress>;
  imageBuild(request: ImageBuildRequest, options?: Partial<CallOptions>): ClientReadableStream<BuildProgress>;
  imageBuild(
    request: ImageBuildRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<BuildProgress>;
  /** Networking */
  networkCreate(
    request: NetworkCreateRequest,
    callback: (error: ServiceError | null, response: NetworkCreateResponse) => void,
  ): ClientUnaryCall;
  networkCreate(
    request: NetworkCreateRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: NetworkCreateResponse) => void,
  ): ClientUnaryCall;
  networkCreate(
    request: NetworkCreateRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: NetworkCreateResponse) => void,
  ): ClientUnaryCall;
  networkRemove(
    request: NetworkRemoveRequest,
    callback: (error: ServiceError | null, response: NetworkRemoveResponse) => void,
  ): ClientUnaryCall;
  networkRemove(
    request: NetworkRemoveRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: NetworkRemoveResponse) => void,
  ): ClientUnaryCall;
  networkRemove(
    request: NetworkRemoveRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: NetworkRemoveResponse) => void,
  ): ClientUnaryCall;
  /** Monitoring (snapshot + ad-hoc ping) */
  listContainers(
    request: ListContainersRequest,
    callback: (error: ServiceError | null, response: ListContainersResponse) => void,
  ): ClientUnaryCall;
  listContainers(
    request: ListContainersRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListContainersResponse) => void,
  ): ClientUnaryCall;
  listContainers(
    request: ListContainersRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListContainersResponse) => void,
  ): ClientUnaryCall;
  pingContainer(
    request: PingContainerRequest,
    callback: (error: ServiceError | null, response: PingContainerResponse) => void,
  ): ClientUnaryCall;
  pingContainer(
    request: PingContainerRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PingContainerResponse) => void,
  ): ClientUnaryCall;
  pingContainer(
    request: PingContainerRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PingContainerResponse) => void,
  ): ClientUnaryCall;
  /** Interactive shell (bidi streaming) */
  containerExec(): ClientDuplexStream<ExecFrame, ExecFrame>;
  containerExec(options: Partial<CallOptions>): ClientDuplexStream<ExecFrame, ExecFrame>;
  containerExec(metadata: Metadata, options?: Partial<CallOptions>): ClientDuplexStream<ExecFrame, ExecFrame>;
}

export const AgentClient = makeGenericClientConstructor(AgentService, "ploydok.agent.v1.Agent") as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): AgentClient;
  service: typeof AgentService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
